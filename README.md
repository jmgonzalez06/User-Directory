# User-Directory
App that fetches user data from a public API, stores it locally in Room Database, and displays it in a clean UI. The app must work offline by showing cached data when there's no internet connection.
